# Ceph v2 Foundation

This unreleased entry adds the Ceph v2 desired-state and operations foundation:
providers, operations, plans, validations, operation runs, drift records, metric
snapshots, orchestrator feature detection, and dashboard/list UI routes.

The existing v1 reflected inventory remains read-only. Package and plugin
versions are unchanged.
